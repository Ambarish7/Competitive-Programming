int Solution::solve(int A) {
    int mod = 10003;
    vector<int> dp(A+1,-0);
    dp[0]=dp[1]=1;
    for(int i = 2 ; i<=A;i++){
        dp[i] = ( dp[i-1]%mod + ((i-1)*dp[i-2])%mod)%mod;
    }
    return dp[A];
}
